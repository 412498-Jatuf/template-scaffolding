### **Agent Skill: JS-ELITE-EXAM-ARCHITECT**

#### **1. Frontmatter**
*   **Paradigm:** Specification-Driven Development (SDD) and Logic-UI Decoupling (MVC).
*   **Standard:** ECMAScript 2024+ (Vanilla JS).
*   **Environment:** Node.js and npm (No Frameworks / No Vite).

#### **2. Purpose & Context**
Expert in modular, accessible, and high-performance web application architecture using native browser APIs. Specialist in complex algorithm resolution and robust Client-Server communication via Fetch.

#### **3. Procedural Workflow**
*   **Phase A (Strategy):** Requirement decomposition. Definition of mathematical models (Matrices, Graphs, etc.) and pure JS logic before touching the DOM.
*   **Phase B (Structure):** Implementation of Semantic HTML5 and advanced accessibility using ARIA attributes (`aria-live`, `aria-label`).
*   **Phase C (Clean Code):** Zero DOM references within logic modules. Use of Classes or Modules to encapsulate state.
*   **Phase D (Asynchrony/Persistence):** Implementation of Fetch services with `try/catch` error handling. Use of `localStorage` for data persistence.